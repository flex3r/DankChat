// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    ext {
        apkName = "DankChat"

        gradle_build_tools_version = '3.5.1'
        kotlin_version = "1.3.50"
        safe_args_version = "1.0.0-alpha09"

        appcompat_version = '1.1.0'
        constraint_version = "1.1.3"
        core_version = '1.2.0-beta01'
        media_version = '1.1.0'
        coroutines_version = '1.3.2'
        koin_version = "2.0.1"
        lifecycle_version = '2.2.0-rc01'
        material_version = '1.2.0-alpha01'
        nav_version = '2.2.0-rc01'
        glide_version = '4.10.0'
        three_ten_abp_version = '1.2.1'
        browser_version = "1.0.0"
        gif_drawable_version = '1.2.19'
        viewpager2_version = '1.0.0-rc01'
        okhttp_version = '4.2.2'
        retrofit_version = '2.6.2'
        moshi_version = '2.6.2'
        url_detector_version = '0.1.21'
        exif_version = '1.1.0-rc01'
        preference_version = '1.1.0'
        recyclerview_version = '1.1.0-rc01'
    }

    repositories {
        google()
        jcenter()
    }

    dependencies {
        classpath "com.android.tools.build:gradle:${gradle_build_tools_version}"
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
        classpath "android.arch.navigation:navigation-safe-args-gradle-plugin:$safe_args_version"
    }
}

allprojects {
    repositories {
        google()
        jcenter()
        mavenCentral()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
